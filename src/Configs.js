let BOARD_ROWS = 20; // Número de linhas
let BOARD_COLS = 20; // Número de colunas
let HOLE_RATE = 0.2; // Proporção de obstáculos em campo de 0 a 1
let LOOP_INTERVAL = 500; // Tempo de execução do Loop em milisegundos
let ALGORITHM = Algorithms.A_STAR;
